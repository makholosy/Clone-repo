[project]
name = "ci-nanocourse-demo"
version = "0.1.0"
description = "Demo project for CI nanocourse"
authors = [{name = "Partner A + Partner B"}]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
my_new_package = "my_new_package.hello:main"
